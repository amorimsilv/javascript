let i;
for (i = 0; i < 10; i++) {
    console.log(i);
    if (i == 5) {
        break;
    }
}

// Novamente a let I fica null
// Na linha 2, e 3, é criado um loop
// na linha 4, é o seguinte, SE (if), o I for IGUAL (==) a 5, ira criar um break (a contagem irá parar no 5)